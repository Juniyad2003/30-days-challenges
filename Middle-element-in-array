#include <iostream> 
using namespace std; 

int mid(int array[], int n) 
{
  int start = 0; 
  int end = n - 1; 

  int mid = (start + end) / 2; 

  cout << array[mid] << endl; 
}
 
int main() 
{ 
  int array[7] = {2, 3, 5, 6, 8, 9, 1}; 

  int n = 7; 

  mid(array, n); 

  return 0; 
}

