#include <iostream>
using namespace std;

bool isEven(int number) 
{

    return (number & 1) == 0;
}

int main() 
{
    int num = 6; 

    if (isEven(num)) {
        cout << num << " is even." << endl;
    } else {
        cout << num << " is odd." << endl;
    }

    return 0;
}

